# File which is referenced in configurations where a path to an existing file must be specified.
# The only purpose of this file is to exist for validation of "file" tagged properties.
# e.g. config.go: KerberosConfig.KeytabPath
